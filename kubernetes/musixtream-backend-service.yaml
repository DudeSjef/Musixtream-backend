apiVersion: v1
kind: Service
metadata:
  name: musixtream-backend-service
  namespace: musixtream
spec:
  selector:
    app: musixtream
    tier: backend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080